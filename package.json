{
  "name": "rally-notification",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:build:app": "tauri build --bundles app --no-sign",
    "dmg:simple": "bash scripts/create-dmg.sh",
    "release:dmg": "npm run tauri:build:app && npm run dmg:simple"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-notification": "^2.0.0",
    "@tauri-apps/plugin-opener": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
